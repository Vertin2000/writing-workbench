# 03_DataRoom — 外部素材层

本层存放按需加载的素材索引和合成数据。它不是每次开工都加载的 Context。

## 本例文件

- `素材总索引.md`：DataRoom 入口。
- `当前数据索引.md`：合成指标字段和使用限制。
- `generated/stage_metrics.csv`：可机器读取的合成指标。
- `generated/stage_metrics.md`：写作时直接引用的指标表。

## 使用规则

只有模块任务书点名时才加载对应素材。写作时不要从记忆补数字。
