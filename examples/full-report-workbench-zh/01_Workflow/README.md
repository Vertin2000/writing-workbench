# 01_Workflow — 写作流程层

本层定义本项目怎么写：先写哪一章，依赖什么素材，单模块调用时按什么步骤走。

## 本例文件

- `结构方案.md`：候选成稿结构、实际写作顺序、推荐方案和最终选择。
- `写作流程.md`：实际写作顺序、单模块调用模板、占位符约定。
- `章节写作看板.md`：模块依赖、必读材料和就绪程度。

## 使用规则

开新模块前先看 `结构方案.md` 和 `章节写作看板.md`。写作时按 `写作流程.md` 的 6 步模板执行。不要默认成稿结构就是实际写作顺序。
